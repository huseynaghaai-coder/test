{
  "name": "mock-api",
  "version": "1.0.0",
 "scripts": {
  "start": "json-server --watch db.json --host 0.0.0.0 --port 10000 --middlewares ./cors.js"
},
  "dependencies": {
    "json-server": "^0.17.4"
  }
}
